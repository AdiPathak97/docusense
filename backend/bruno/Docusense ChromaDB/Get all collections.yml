info:
  name: Get all collections
  type: http

http:
  method: GET
  url: http://localhost:8000/api/v1/collections
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
